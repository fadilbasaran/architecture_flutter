import 'package:flutter_test/flutter_test.dart';

void main() {
  setUp(
    () {}, //ilk çalıştığınızda buraya gelir
  );
  //MARK:Test1
  test(
    'User-Login Fail Test',
    () {
      const isUserLogin = true;
      expect(isUserLogin, isTrue);
    },
  );
  //MARK:Test2
    test(
    'User-Login Fail Test',
    () {
      const isUserLogin = true;
      expect(isUserLogin, isTrue);
    },
  );
  //MARK:Test3
    test(
    'User-Login Fail Test',
    () {
      const isUserLogin = true;
      expect(isUserLogin, isTrue);
    },
  );
  //MARK:Test4
    test(
    'User-Login Fail Test',
    () {
      const isUserLogin = true;
      expect(isUserLogin, isTrue);
    },
  );
}
